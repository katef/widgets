/* $Id$ */

section.page {
	padding: $grid-baseline 1em;
}

header, nav.menu, section.page, footer {
	grid-container(0 1em, 0);
}

nav.menu {
/* XXX:
	margin-top:   -$grid-baseline;
	margin-left:  -1em;
	margin-right: -1em;
	margin-bottom: 0;
*/
}

nav#monthindex {
	display: block;

	ol.pages {
		float: left;
	}

	> a {
		float: right;
	}
}

body section.page {
/* XXX:
	position: relative;
*/
}

nav.sidebar {
	font-size:   ($grid-basefont - 1px);
	top:      ($grid-baseline * 7);
/* TODO:
	width:    ($grid-width    * 3);
	right:    1em;
	position: absolute;
*/
	display: none;
}

