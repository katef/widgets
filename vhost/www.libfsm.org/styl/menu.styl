/* $Id$ */

@import "theme.styl";

nav.menu {
	min-height: ($baseline * 10); /* XXX: temp */

	ul {
		margin: 0;
		list-style-type: none;
		width: 50%; /* XXX: temp */
		position: absolute;
		right: 0;
	}
}

