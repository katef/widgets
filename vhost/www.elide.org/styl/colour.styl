/* $Id: elide.css 571 2013-07-03 22:32:11Z kate $ */

@import "misc.styl";

$accent = #f22;

body, td {
	background-color: #fff;
	color: #000;
}

a code, a, a:link, a:visited {
	color: $accent;
	text-decoration: none;

	*[title].important {
		border-color: @color;
	}
}

a:hover code, a:hover {
	text-decoration: none;
	color: #000;

	*[title].important {
		border-color: @color;
	}
}

section.page > dl.index dt a:hover {
	color: inherit;
	background-color: #eee;
}

article.entry {
	a:hover time {
		color: inherit;
	}

	time[pubdate] {
		color: #ccc;
	}
}

*[title].important {
	border-bottom: dotted 1px #888;
}

*[title] {
	cursor: help;
}

a *[title] {
	cursor: inherit;
}

::-moz-selection {
	background-color: #ddd;//lighten($accent, 15%);
}

::selection {
	background-color: #ddd;//lighten($accent, 15%);
}

